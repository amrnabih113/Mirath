class LibraryData {
  final int listsCount, createdCount, savedCount, projectsCount;

  LibraryData({
    required this.listsCount,
    required this.createdCount,
    required this.savedCount,
    required this.projectsCount,
  });
}
